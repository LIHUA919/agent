# AI Agent with Computer Use

一个基于Java Spring Boot后端和React前端的AI Agent系统，具备Computer Use功能，可以通过自然语言控制计算机操作。

## 项目架构

```
ai-agent/
├── backend/                    # Spring Boot后端服务
│   ├── src/main/java/
│   │   └── org/example/aiagent/
│   │       ├── controller/     # REST API控制器
│   │       ├── service/        # 业务逻辑服务
│   │       ├── dto/           # 数据传输对象
│   │       ├── computeruse/   # Computer Use功能
│   │       └── service/ai/    # AI集成服务
│   └── src/main/resources/
│       └── application.properties  # 应用配置
├── chat-agent-ui/             # React前端应用
│   ├── src/
│   │   ├── App.js            # 主应用组件
│   │   ├── Chat.js           # 聊天界面组件
│   │   └── Chat.css          # 样式文件
│   └── package.json          # 前端依赖配置
├── start-backend.bat         # 后端启动脚本
├── start-frontend.bat        # 前端启动脚本
└── README.md                 # 项目文档
```

## 功能特性

🤖 **AI对话功能** - 智能聊天交互
🖱️ **鼠标控制** - 点击、移动等鼠标操作
⌨️ **键盘输入** - 自动文本输入
📸 **屏幕截图** - 屏幕捕获和分析
🚀 **应用启动** - 打开各种应用程序
🧠 **智能意图识别** - 自动识别用户操作意图
🌐 **RESTful API** - 标准化API接口
⚛️ **React前端** - 现代化用户界面

## 环境要求

### 后端
- Java 21+
- Maven 3.6+

### 前端
- Node.js 16+
- npm 或 yarn

### 可选（AI功能增强）
- OpenAI API Key（用于高级AI功能）

## 快速开始

### 1. 启动后端服务

```bash
# Windows用户
start-backend.bat

# 或者手动启动
cd backend
mvn spring-boot:run
```

后端服务将在 `http://localhost:8080` 启动

### 2. 启动前端服务

```bash
# Windows用户
start-frontend.bat

# 或者手动启动
cd chat-agent-ui
npm install
npm start
```

前端服务将在 `http://localhost:3000` 启动

### 3. 配置AI服务（可选）

设置环境变量以启用高级AI功能：

```bash
# OpenAI API配置
export AI_API_KEY="your-openai-api-key"
export AI_API_URL="https://api.openai.com/v1/chat/completions"
```

## 使用示例

启动服务后，在浏览器中打开 `http://localhost:3000`，然后可以尝试以下命令：

### AI对话
```
你好，请介绍一下你自己
今天天气怎么样
帮我写一首诗
```

### Computer Use功能
```
帮我截个图
点击屏幕中心
输入"Hello World"
打开记事本
将鼠标移动到右上角
```

### 混合命令
```
帮我打开记事本并输入一段话
截个图然后告诉我屏幕上有什么
```

## API接口

### 聊天接口
```
POST http://localhost:8080/api/chat
Content-Type: application/json

{
  "userInput": "你的消息内容"
}

Response:
{
  "response": "AI回复内容"
}
```

### 健康检查
```
GET http://localhost:8080/api/health

Response:
"AI Agent is running"
```

## 技术栈

### 后端
- **Spring Boot 3.2.0** - Java Web框架
- **OkHttp** - HTTP客户端
- **Gson** - JSON处理
- **JNA** - 系统集成库
- **AWT Robot** - 屏幕和输入控制

### 前端
- **React 18** - 用户界面框架
- **Axios** - HTTP客户端
- **CSS3** - 样式设计

## 开发指南

### 添加新的Computer Use功能

1. 在 `ComputerUseService.java` 中添加新的处理方法
2. 在 `executeCommand()` 方法中添加命令识别逻辑
3. 测试新功能

### 集成其他AI服务

1. 实现 `AiService` 接口
2. 在 `application.properties` 中配置相关参数
3. 更新依赖配置

### 自定义前端界面

1. 修改 `chat-agent-ui/src/Chat.js`
2. 更新 `Chat.css` 样式
3. 添加新的React组件

## 安全注意事项

⚠️ **重要提醒**：

1. **API密钥安全** - 请妥善保管AI API密钥
2. **权限控制** - 确保程序有适当的系统权限
3. **屏幕隐私** - 屏幕截图可能包含敏感信息
4. **网络安全** - 在生产环境中配置适当的CORS和安全策略
5. **操作验证** - 重要操作前建议增加确认机制

## 故障排除

### 常见问题

1. **后端启动失败**
   ```
   检查Java和Maven版本
   确保端口8080未被占用
   查看logs目录下的日志文件
   ```

2. **前端连接失败**
   ```
   确保后端服务已启动
   检查CORS配置
   验证API端点URL
   ```

3. **Computer Use功能异常**
   ```
   Windows: 可能需要管理员权限
   macOS: 需要在"隐私与安全性"中授权
   Linux: 确保有X11显示权限
   ```

4. **AI功能不工作**
   ```
   检查AI_API_KEY环境变量
   验证网络连接
   确认API配额和访问权限
   ```

## 部署指南

### Docker部署（推荐）

```bash
# 构建后端Docker镜像
cd backend
docker build -t ai-agent-backend .

# 运行后端容器
docker run -d -p 8080:8080 -e AI_API_KEY=your-key ai-agent-backend

# 构建前端
cd ../chat-agent-ui
npm run build

# 使用nginx或其他Web服务器托管前端build目录
```

### 传统部署

1. 打包后端：`cd backend && mvn package`
2. 运行JAR：`java -jar target/backend-1.0.0.jar`
3. 构建前端：`cd chat-agent-ui && npm run build`
4. 配置Web服务器指向build目录

## 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 许可证

MIT License

## 更新日志

### v2.0.0
- 重构为前后端分离架构
- 添加Spring Boot后端服务
- 集成React前端界面
- 实现RESTful API接口
- 增强AI服务集成
- 添加完整的配置管理

### v1.0.0
- 初始版本发布
- 基本的Computer Use功能
- 支持中英文命令
- 集成OpenAI Vision API